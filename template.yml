---
- name: Update File Named 'rohit'
  hosts: all
  become: yes
  vars:
    file_path: "/media/rohit"
    content_line: "This is my existing file and i am modifying it."

  tasks:
    - name: Ensure content is present in the file
      lineinfile:
        path: "{{ file_path }}"
        line: "{{ content_line }}"
        create: yes
...